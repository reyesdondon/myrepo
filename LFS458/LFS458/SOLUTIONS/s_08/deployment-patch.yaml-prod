apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: myapp
  name: myapp
spec:
  replicas: 3                    # staging uses 3 replicas
  template:
    spec:
      containers:
      - name: nginx
        image: nginx:1.22

